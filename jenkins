pipeline{
   agent any
   stages{
     stage("git checkout"){
	steps{
git credentialsId: 'skumarre', url: 'https://github.com/skumarre/jenkinstest/'
	}
 	stage("maven bulid"){
      steps{
         sh "mvn clean package"
       } 
      }   
     }	
   }
}
